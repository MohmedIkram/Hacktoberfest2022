#include <stdio.h>

#include <conio.h>
int main()
{
int i=1,j=1;
int n=1,rows;
printf("enter number of rows :");
scanf("%d",&rows);
for(i=1;i<=rows;i++
{
for(j=1;j<=i;j++)
{
printf("%d ", n );
n++;
}
printf("\n");
}
return 0;
}
